import React from 'react';
import ReactDom from 'react-dom';

const Index = () => {
    return <div>
        Application works
    </div>
}
ReactDom.render(<Index></Index>, document.getElementById('main'))
